<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:jpa="http://www.springframework.org/schema/data/jpa"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context
                           http://www.springframework.org/schema/context/spring-context-4.2.xsd
                           http://www.springframework.org/schema/data/jpa
                           http://www.springframework.org/schema/data/jpa/spring-jpa.xsd">

    <context:component-scan base-package="com.epam.rd.backend.core.service"/>
    <!--TODO !!! should I declare all services as bean-->
    <context:property-placeholder location="classpath:app-config.properties"/>
    <jpa:repositories base-package="com.epam.rd.backend.core.repository"/>

    <bean class="org.springframework.context.support.PropertySourcesPlaceholderConfigurer"/>

    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <p:url value="${db.url}"/>
        <p:driverClassName value="${db.driver}"/>
        <p:username value="${db.username}"/>
        <p:password value="${db.password}"/>
    </bean>

    <bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <p:dataSource ref="dataSource"/>
        <p:packagesToScan value="com.epam.rd.backend.core.model"/>
        <property name="jpaVendorAdapter">
            <bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter"/>
        </property>
        <property name="jpaProperties">
            <props>
                <prop key="hibernate.dialect">"${hibernate.dialect}"</prop>
                <prop key="hibernate.show_sql">"${hibernate.show_sql}"</prop>
                <prop key="hibernate.format_sql">"${hibernate.format_sql}"</prop>
                <prop key="hibernate.hbm2ddl.auto">"${hibernate.hbm2ddl.auto}"</prop>
            </props>
        </property>
        <property name="persistenceProvider">
            <bean class="org.hibernate.jpa.HibernatePersistenceProvider"/>
        </property>
    </bean>

    <bean class="org.springframework.orm.jpa.JpaTransactionManager">
        <p:entityManagerFactory value="#{entityManagerFactory.getObject()}"/>
    </bean>

</beans>